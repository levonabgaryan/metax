[SERVICE]
    Flush           1
    Daemon          Off
    Log_Level       info

# Docker `fluentd` logging driver speaks the Fluent Forward protocol.
[INPUT]
    Name            forward
    Listen          0.0.0.0
    Port            24224

[OUTPUT]
    Name            stdout
    Match           *
